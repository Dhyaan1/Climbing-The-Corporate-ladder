import { defineConfig } from 'vite'

// https://vitejs.dev/config/
export default defineConfig({
  base: '/Climbing-The-Corporate-ladder/',
  build: {
    outDir: 'dist',
    assetsDir: 'assets',
  }
}) 
